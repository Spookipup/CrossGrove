

Ponder.registry((event) => {
    // event.create("crossroads:gear_base").scene("intro_to_rotary", "Introduction to Crossroads Rotary", (scene, util) => {
    //     scene.showStructure();
    //     scene.showBasePlate();
    //     scene.configureBasePlate(4, 4, 10);
    //     let world = scene.world;
    //     world.setBlock([2, 1, 2], Block.id("crossroads:master_axis").with("facing", "up").getBlockState(), false);
    // });
});